# Detector specs (shouldn't change)
detector_cols_spec: 1130      # also those outside ROI
detector_rows_spec: 1548      # also those outside ROI
detector_width_spec: 30.2     # cm, also outside ROI
detector_height_spec: 30.7    # cm, also outside ROI
detector_pixel_spec: 0.0198   # cm

# Setup properties (Measurements & Settings)
# Expected to be the same for all measurements. If you change these half-way, 
# you'll have to make a new yaml for the second part. If you want to change them
# throughout your experiments, you'll have to rework the codebase a bit.
source_radius: 95 
detector_radius: 30
detector_cols: 1130          # including ROI
detector_rows: 1548          # including ROI
column_inner_diameter: 14.2   # cm
column_outer_diameter: 15   # cm

# Calibration properties
root: "U:\\Xray RPT ChemE\\X-ray\\Xray_data\\2024-05-16 Lisanne\\VROI190_1320" 
calibration_folder: pre_proc_VROI190_1320_needles_10degsec
rotation:
  start: 36
  stop: 1190
frames:
  start: 60
  n: 3
reconstruction:   # for part02
  step: 120    # smaller step = more frames = better, slower reconstruction
# Some versions of preprocessing mirror the raw images, some don't. Has a major
# impact on the calibration. Exact 'error' that you can recognize in the plots
# will still be added to README (TODO).
images_mirrored: True
